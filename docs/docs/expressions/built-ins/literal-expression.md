# Literal Expression

`LiteralExpression` provided by the library can be use cased for passing a static string to your parent expression. \
Internally it works thanks to `Expression` method `useStringParametersAsArguments`.

For potential use cases visit [quickstart page](/string-language/quickstart).